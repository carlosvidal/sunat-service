name: Publicar imagen

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE: ${{ github.repository }}

jobs:
  imagen:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=sha,format=short

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Paso de despliegue: completar según dónde corra el servicio.
  # Ejemplos habituales:
  #   - VPS con Docker:   ssh + docker compose pull && docker compose up -d
  #   - Kubernetes:       kubectl set image deployment/sunat-service …
  #   - Fly.io / Render:  su acción oficial, apuntando a la imagen publicada
  #
  # Recordar levantar DOS procesos con la misma imagen:
  #   API     -> node dist/index.js
  #   worker  -> node dist/worker.js
  # y ejecutar `node dist/index.js --migrate` antes de arrancar tras un cambio de esquema.
